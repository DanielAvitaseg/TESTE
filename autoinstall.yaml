#cloud-config
autoinstall:
  version: 1

  # CONFIGURAÇÃO DE ARMAZENAMENTO (Criptografia e LVM)
  storage:
    wipe: all
    layout:
      name: lvm
      match:
        path: /dev/nvme0n1
      password: 'Senha2024'
  
  # CONFIGURAÇÕES DE SISTEMA
  identity:
    hostname: daniel_sammartano
    username: Daniel.Sammartano # NOME FORNECIDO
    password: 'Senha2024'
    
  locale: pt_BR
  timezone: America/Sao_Paulo
  keyboard:
    layout: br

  # INSTALAÇÃO DE APLICATIVOS (APT e SNAP)
  packages:
    - docker.io
    - git
    - gimp
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg
    - lsb-release
    - wget
    - libglib2.0-bin 

  snaps:
    - name: spotify
      channel: stable
    - name: postman
      channel: stable
    - name: slack
      channel: stable
    - name: code
      channel: stable
      classic: true
    - name: dbeaver-ce
      channel: stable
      
  # Configurações adicionais
  codecs:
    install: true
  drivers:
    install: true
  updates: all

  # COMANDOS DE PÓS-INSTALAÇÃO (ROOT)
  late-commands:
    # Atualiza repositórios
    - curtin in-target -- apt update
    # Atualiza pacotes instalados
    - curtin in-target -- apt upgrade -y
    # CORREÇÃO: Usando o nome de usuário em minúsculas (daniel.sammartano)
    - curtin in-target -- usermod -aG docker daniel.sammartano 
      
  # AÇÃO FINAL
  power-state:
    delay: 5
    mode: reboot
